class TickCounter_hl { 
    //Auto-generated fields of the TickCounter_hl class:
    Main m;
    int ticks;
    int speed;
    
    //Auto-generated constructor:
    //Auto-generated specifications of the constructor TickCounter_hl:
    ensures Perm(m, write) **
            Perm(ticks, write) **
            Perm(speed, write) **
            (m == m_param) ;
    TickCounter_hl(Main m_param){
        m = m_param;
    }
    
    
    //Auto-generated functions:
    //Auto-generated specifications of the function rand__randomize_0:
    pure int rand__randomize_0();
    
    //Auto-generated specifications of the function run:
    context Perm(m, read) **
            (m != null) **
            Perm(m.hl, read) **
            (m.hl == this) ;
    void run(){
        lock m;
        loop_invariant true ** 
                Perm(m, read) ** 
                (m != null) ** 
                held(m)  ** 
                m.global_permission_invariant() ** 
                (m.hl == this) ;
        while (true) {
            m.process_state = m.process_state[2 -> 10];
            m.event_state = m.event_state[10 -> 1];
            loop_invariant true ** 
                    Perm(m, read) ** 
                    (m != null) ** 
                    held(m)  ** 
                    m.global_permission_invariant() ** 
                    (m.hl == this) ;
            while (m.process_state[2] != -1 || m.event_state[10] != -2) {
                unlock m;
                lock m;
            }
            speed = rand__randomize_0();
            loop_invariant true ** 
                    Perm(m, read) ** 
                    (m != null) ** 
                    held(m)  ** 
                    m.global_permission_invariant() ** 
                    (m.hl == this) ;
            while (|m.speed_hl.buffer| >= 16) {
                m.process_state = m.process_state[2 -> 4];
                loop_invariant true ** 
                        Perm(m, read) ** 
                        (m != null) ** 
                        held(m)  ** 
                        m.global_permission_invariant() ** 
                        (m.hl == this) ;
                while (m.process_state[2] != -1 || m.event_state[4] != -2) {
                    unlock m;
                    lock m;
                }
            }
            m.speed_hl.fifo_write(speed);
        }
        unlock m;
    }
    
}

